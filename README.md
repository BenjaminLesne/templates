Boilerplate for my react applications.

prepush hook fails à cause de pnpx vitest run
=> configurer vitest afin qu'il ne run pas les tests playwright

pre commmit > tsc se lance sur les librairies node_modules

Note:
vite X
vitest X
playwright X
pnpm X
nvim
typescript X
eslint X
=> for typescript
prettier X
Git hooks (pre commit etc)
pre commit:
=>husky
=>lint-staged
xstate


pre push:
todo+++
